# JIRA Configuration
JIRA_URL=https://your-domain.atlassian.net
JIRA_USER_EMAIL=your-jira-api-email@example.com
JIRA_API_TOKEN=your_jira_api_token_here
JIRA_PROJECT_KEY=PROJ # e.g., The key for your JIRA project
JIRA_ISSUE_TYPE=Task # e.g., Task, Bug, Story (must exist in the project)

# Note: COMPOSER_PROJECT_PATH and NPM_PROJECT_PATH are no longer needed
# as the path is passed directly as a command-line argument. 